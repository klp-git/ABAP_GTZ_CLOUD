projection;     //implementation in class ZBP_C_SAMPLESCHEDULE unique;
strict ( 2 );
use draft;
define behavior for ZC_SAMPLESCHEDULE alias ZcSampleschedule
use etag

{
  use create;
  use update;
  use delete;

  use action createSampleSchedule;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}